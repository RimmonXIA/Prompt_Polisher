Pass=true when: required structure is present (e.g. if <user_context> opens it closes), the draft is not trivially empty, the author's declared methodology remains represented inside <user_context>, and there is no obvious harmful instruction. CoT, reframe-first, Decode-Reframe-Response, or similar scaffolds must NOT alone cause failure. Fail only for concrete structural or safety problems (say which in feedback and issues).